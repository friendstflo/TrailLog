<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingHorizontal="12dp"
    android:paddingVertical="10dp">

    <ImageView
        android:id="@+id/iv_tool_icon"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:contentDescription="@null"
        android:scaleType="fitCenter"
        tools:src="@drawable/ic_tool_rake" />

    <TextView
        android:id="@+id/tv_tool_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:layout_weight="1"
        android:ellipsize="end"
        android:maxLines="1"
        android:textAppearance="?attr/textAppearanceTitleMedium"
        tools:text="Shovels" />

    <!-- Matching dark green − / + (primary #1B5E20; same weight) -->
    <ImageButton
        android:id="@+id/btn_minus"
        android:layout_width="44dp"
        android:layout_height="44dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/tools_decrement"
        android:padding="10dp"
        android:scaleType="fitCenter"
        android:src="@drawable/ic_minus"
        android:tint="@color/primary" />

    <TextView
        android:id="@+id/tv_tool_count"
        android:layout_width="48dp"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:textAppearance="?attr/textAppearanceHeadlineSmall"
        android:textStyle="bold"
        tools:text="0" />

    <ImageButton
        android:id="@+id/btn_plus"
        android:layout_width="44dp"
        android:layout_height="44dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/tools_increment"
        android:padding="10dp"
        android:scaleType="fitCenter"
        android:src="@drawable/ic_plus"
        android:tint="@color/primary" />

</LinearLayout>
